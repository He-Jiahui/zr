export enum Access {
    PUBLIC = "pub",
    PRIVATE = "pri",
    PROTECTED = "pro",
}

export enum PropertyType {
    GET = "get",
    SET = "set",
    GET_SET = "get_set",
}

export enum MetaFunctionType {
    CONSTRUCTOR = 1,
    ADD = 2,
    SUB = 3,
    MUL = 4,
    DIV = 5,
    MOD = 6,
    NEG = 7,
    COMPARE = 8,
    TO_BOOL = 9,
    TO_STRING = 10,

}
